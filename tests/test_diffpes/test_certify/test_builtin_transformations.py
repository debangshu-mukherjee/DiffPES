"""Exercise built-in transformation registration through the public boundary.

The public registration tests live in test_models because the private
transformation vocabulary has no independently supported API.
"""
