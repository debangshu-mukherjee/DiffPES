"""Exercise retarded validation through the public types surface.

Test Methodology
----------------
The generalized spectral tests own the numerical validation cases. This
module preserves the mirrored source-to-test layout for the focused carrier.
"""


class TestRetardedvalidationreport:
    """Verify ownership of ``diffpes.types.RetardedValidationReport``.

    Keep the carrier linked to its focused mirrored test module.
    """


class TestMakeRetardedValidationReport:
    """Verify ownership of ``diffpes.types.make_retarded_validation_report``.

    Keep the factory linked to its focused mirrored test module.
    """
