"""Exercise certificate decoding through the public storage boundary.

The storage tests cover document validation, numerical decoding, and carrier
reconstruction because the decoder has no independently supported API.
"""
