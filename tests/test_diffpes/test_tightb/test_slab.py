"""Validate the slab module.

The cases use analytic values, invariants, and finite differences.
"""


class TestGenSlab:
    """Mirror coverage for :func:`diffpes.tightb.gen_slab`.

    The detailed slab cases check open-boundary construction, layer metadata,
    and model closure.
    """
